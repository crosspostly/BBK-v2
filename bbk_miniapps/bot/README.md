# Telegram Bot for Booking (Panda Rolls)

Этот бот предназначен для записи блогеров на свободные даты в календаре «Панда Ролс» через Google Таблицы.

## Основные возможности

- **Бронирование дат:** Просмотр доступных дат с автоматическим расчетом свободных мест (столбцы B-E в таблице).
- **Защита от дублей:** Бот проверяет, записан ли уже пользователь на выбранную дату. Один пользователь может занять только одно место на конкретную дату.
- **Пагинация календаря:** Удобный выбор дат с переключением страниц (**6 дат на страницу**), что гарантирует корректное отображение навигационных кнопок на любых устройствах.
- **Интеграция с Google Sheets:**
    - Поиск таблицы осуществляется по ID (надежная привязка, не зависящая от имени файла).
    - Лист ищется по имени `Расписание` (или используется первый лист, если такого имени нет).
- **Автоматические напоминания:** Бот планирует уведомления (время выделяется жирным):
    - За 1 день до события (12:00 МСК).
    - В день события (08:00 МСК).
- **Восстановление при перезагрузке:** Бот автоматически восстанавливает все напоминания из Google Таблицы при каждом запуске.
- **Маршруты и карты:** После записи пользователь получает кнопки с прямыми ссылками на Яндекс.Карты и 2GIS.
- **Мои события:** Единый центр управления записями. Показывает список активных броней и позволяет отменять конкретные даты нажатием одной кнопки.

## Технические детали реализации

### 1. Формат хранения данных в таблице
Для обеспечения надежности и возможности восстановления напоминаний, данные в ячейки (столбцы B, C, D, E) записываются в две строки:
1.  **Ссылка на профиль:** `https://t.me/username` (для удобства администратора).
2.  **ID пользователя:** `(ID: 123456789)` (скрытый идентификатор для работы бота).

*Это позволяет боту идентифицировать пользователя даже после полной очистки оперативной памяти сервера.*

### 2. Геолокация (Ячейка F2)
Бот парсит ячейку **F2** на листе «Расписание». Ожидаемый формат в ячейке (3 строки):
- Строка 1: Текстовый адрес (отображается в сообщении **жирным**).
- Строка 2: Прямая ссылка на Яндекс.Карты (кнопка "🗺️ Yandex Maps").
- Строка 3: Прямая ссылка на 2GIS (кнопка "📍 2GIS").

### 3. Планировщик задач (APScheduler)
Используется `AsyncIOScheduler`. Несмотря на использование MemoryJobStore, надежность достигнута за счет функции `restore_reminders_from_sheet`, которая сканирует таблицу при старте и заново «взводит» все будильники.

## Установка и запуск

1.  **Окружение:** Используйте Python 3.10+.
2.  **Зависимости:** `aiogram`, `gspread`, `oauth2client`, `apscheduler`, `pytz`.
3.  **Учетные данные:** Файл `credentials.json` должен находиться в папке с ботом.
4.  **Запуск через Systemd:**
    Сервис: `bbk-bot.service`
    Команды управления:
    ```bash
    sudo systemctl restart bbk-bot.service  # Перезапуск
    systemctl status bbk-bot.service        # Статус
    journalctl -u bbk-bot.service -f        # Логи в реальном времени
    ```

## Конфигурация
Ключевые параметры находятся в файле `aiogram_webhook_bot.py`:
- `TOKEN`: Токен бота.
- `SPREADSHEET_ID`: ID Google Таблицы.
- `PAGE_SIZE`: Количество дат на одной странице (по умолчанию 6).

## Контакты и поддержка
Разработано для проекта ББК (Mini Apps / Bot).